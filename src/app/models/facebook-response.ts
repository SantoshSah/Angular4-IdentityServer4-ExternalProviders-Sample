export class FacebookResponse {
    userID ? : number;
    name ? : string;
    email ? : string;
    status ? : string;
    accessToken ? : string;
    expiresIn ? : number;
    signedRequest ? : string;
}